# Token del bot, obtenido de @BotFather en Telegram
BOT_TOKEN=

# URL de conexión a PostgreSQL.
# En Railway, si añades el plugin de PostgreSQL al proyecto, esta variable
# se genera automáticamente y puedes referenciarla como ${{Postgres.DATABASE_URL}}
# en la configuración del servicio del bot.
DATABASE_URL=
